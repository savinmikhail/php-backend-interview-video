# Визуальный план вопроса 26 — «Authentication и Authorization»

Статус: `APPROVED`  
Сложность: `S`  
Бюджет реализации: `15 минут`  
Shorts: `да, короткий`

## Источники и границы

- `questions.txt`, пункт 38.
- `transcription.txt`, `55:21–57:21`.
- [OWASP — Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html).
- [OWASP — Authorization Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html).

`55:21–56:26` — разговор об образовании и атмосфере: оставить базовый
фиолетовый экран и аватары. `56:26–56:38` удалено по редакционному решению.
`56:38–56:53` остаётся на базовой сцене; карточка появляется на полной
формулировке вопроса.

## Аудит содержания

Ответ корректный.

### Намеренно не показываем

- OAuth/OIDC, JWT и session lifecycle: это другие вопросы.
- RBAC/ABAC/ReBAC и детальные security-рекомендации.
- Отдельный тезис про проверку permission на каждом запросе: нижняя плашка
  удалена по просьбе пользователя, чтобы экран держал только основной контраст.

## Задача визуализации

Закрепить две разные проверки — identity и permission — одним читаемым
pipeline без дублирования всего ответа.

## Состояние 1 — вопрос

Таймкод: source `56:53–56:58` / review `41:10–41:15`.

Текст: `Чем аутентификация от авторизации отличается?`

## Состояние 2 — два разных вопроса

Таймкод: source `56:58–57:21` / review `41:15–41:38`.

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 26/30  Authentication ≠ Authorization                        │
├─────────────────────────────┬────────────────────────────────┤
│ AUTHENTICATION · AuthN      │ AUTHORIZATION · AuthZ          │
│ Кто ты?                     │ Что тебе разрешено?            │
│ credentials → identity      │ identity + policy → allow/deny│
├──────────────────────────────────────────────────────────────┤
│ Михаил · отвечает                       Интервьюер · слушает │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 26/30 · AuthN vs AuthZ      │
├──────────────────────────────┤
│ AUTHN · кто ты?             │
│ credentials → identity      │
├──────────────────────────────┤
│ AUTHZ · что разрешено?      │
│ policy → allow / deny       │
└──────────────────────────────┘
```

## Финальный экранный текст

| Элемент | Текст |
|---|---|
| Вопрос | `Чем аутентификация от авторизации отличается?` |
| AuthN | `Кто ты?` |
| AuthZ | `Что тебе разрешено?` |

## Анимация

- Карточка вопроса сменяется двумя колонками; внутри ответа сначала читается
  identity, затем отдельная policy.

## Shorts

- Хук: `Токен отвечает не на оба вопроса сразу`.

## Материалы и производство

- Использовать две колонки и один policy-flow.
- Новые изображения, исправление и досъёмка не нужны.

## Ревью

- [ ] Базовая сцена покрывает нетехнический разговор до `56:26`.
- [ ] Карточка вопроса стоит на `41:10–41:15` review.
- [ ] AuthN/AuthZ не смешаны с issuance токена.
- [ ] Один экран читается в обоих форматах.
- [ ] Таймкоды сверены.
- [ ] Пользователь принял план.
