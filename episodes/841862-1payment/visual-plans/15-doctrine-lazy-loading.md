# Визуальный план вопроса 15 — «Lazy loading — какие плюсы?»

Статус: `APPROVED`  
Сложность: `L`  
Бюджет реализации: `35–50 минут`  
Shorts: `да`

## Источники и границы

- `questions.txt`, пункт 24.
- `transcription.txt`, `38:01–38:52`; карточка вопроса сверена по исходному
  звуку и идёт `38:01–38:05` (`24:15–24:19` review).
- Переход с пользы на N+1 сверен по исходному звуку: `38:11.07`
  (`24:25.07` review).
- [Doctrine ORM — Working with Objects](https://www.doctrine-project.org/projects/doctrine-orm/en/current/reference/working-with-objects.html).
- [Doctrine ORM — DQL fetch joins](https://www.doctrine-project.org/projects/doctrine-orm/en/3.6/reference/dql-doctrine-query-language.html).
- [Doctrine ORM — Extra Lazy Associations](https://www.doctrine-project.org/projects/doctrine-orm/en/3.6/tutorials/extra-lazy-associations.html).

## Аудит содержания

| Тезис | Оценка | Действие |
|---|---|---|
| Lazy не грузит relation заранее | корректно | показать proxy/collection до обращения |
| Перебор может породить N+1 | корректно | показать один запрос списка и N запросов связей |
| Отличие EXTRA_LAZY не вспомнил | неполный ответ | дать короткое уточнение с `count()` без полной загрузки |

Не показываем все fetch modes, hydration и настройку mapping.

### Намеренно не показываем

- Полный API `PersistentCollection` и все операции EXTRA_LAZY.
- Настройку associations атрибутами/XML/YAML: она не помогает понять механизм.

## Задача визуализации

Показать, когда отложенная загрузка экономит ресурсы, когда превращается в
N+1 и какую проблему отдельно решает EXTRA_LAZY.

## Состояния

| № | Таймкод | Функция |
|---|---|---|
| 1 | `38:01–38:05` | показать точную формулировку вопроса |
| 2 | `38:05–38:11.07` | кодом показать пользу lazy loading |
| 3 | `38:11.07–38:20` | анимировать появление N дополнительных SQL-запросов |
| 4 | `38:20–38:25` | заменить скрытые запросы явным fetch join |
| 5 | `38:25–38:52` | объяснить EXTRA_LAZY |

```mermaid
flowchart LR
  Q["1 · Вопрос"] --> L["2 · Нет обращения — нет SQL"] --> N["3 · N+1"] --> F["4 · Fetch join"] --> E["5 · EXTRA_LAZY"]
```

## Состояние 1 — вопрос

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 15/30  Lazy loading — какие плюсы?                           │
├──────────────────────────────────────────────────────────────┤
│ Михаил · слушает                      Интервьюер · говорит  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 15/30                       │
│ Lazy loading — какие плюсы? │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Состояние 2 — relation не используется

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 15/30  Не используем relation — не загружаем её              │
├─────────────────────────────┬────────────────────────────────┤
│ $order = $orders->find(42); │ SQL log                        │
│ echo $order->getNumber();   │ SELECT ... orders WHERE id=42 │
│ // getItems() не вызывается │ items: 0 queries               │
├─────────────────────────────┴────────────────────────────────┤
│ Нет обращения к relation → нет лишнего SELECT               │
├──────────────────────────────────────────────────────────────┤
│ Михаил · говорит                      Интервьюер · слушает  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 15/30 · LAZY                │
├──────────────────────────────┤
│ $order = find(42)           │
│ echo getNumber()            │
├──────────────────────────────┤
│ orders: SELECT ×1           │
│ items: SELECT ×0            │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Состояние 3 — N+1

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 15/30  Скрытая цена lazy loading: N+1                        │
├─────────────────────────────┬────────────────────────────────┤
│ $orders = findRecent();     │ SQL log                        │
│ foreach ($orders as $order) │ SELECT ... FROM orders        │
│   foreach (getItems() ...)  │ SELECT ... WHERE order_id=42  │
│                             │ SELECT ... WHERE order_id=43  │
│                             │ SELECT ... WHERE order_id=44  │
├──────────────────────────────────────────────────────────────┤
│ Счётчик: 1 → 2 → 3 → … → 1 + N                              │
├─────────────────────────────┴────────────────────────────────┤
│ Михаил · говорит                      Интервьюер · слушает  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 15/30 · N+1                 │
├──────────────────────────────┤
│ foreach orders              │
│   foreach getItems()        │
├──────────────────────────────┤
│ SQL log                     │
│ orders · 1 query            │
│ items  · +N queries         │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Состояние 4 — явный fetch join

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 15/30  Relation нужна всем — загружаем явно                  │
├─────────────────────────────┬────────────────────────────────┤
│ $orderRepository            │ SQL log                        │
│  ->findRecentWithItems();   │ SELECT o.*, i.*               │
│                             │ FROM orders o JOIN items i …  │
│ внутри OrderRepository:     │                                │
│ ->addSelect('i')            │                                │
│ ->leftJoin('o.items', 'i')  │                                │
├─────────────────────────────┴────────────────────────────────┤
│ Relation нужна всем → один явный запрос                      │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 15/30 · Fetch join          │
├──────────────────────────────┤
│ findRecentWithItems()       │
│                             │
│ addSelect('i')              │
│ leftJoin('o.items', 'i')    │
├──────────────────────────────┤
│ SQL log: 1 query            │
└──────────────────────────────┘
```

## Состояние 5 — EXTRA_LAZY

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 15/30  Уточнение ответа: EXTRA_LAZY                          │
├─────────────────────────────┬────────────────────────────────┤
│ LAZY                        │ EXTRA_LAZY                     │
│ count($order->items)        │ count($order->items)           │
│ → загрузить всю collection  │ → SELECT COUNT(...)           │
│                             │ collection не загружена        │
├─────────────────────────────┴────────────────────────────────┤
│ Полезно для очень больших коллекций                         │
├──────────────────────────────────────────────────────────────┤
│ Михаил · говорит                      Интервьюер · слушает  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 15/30 · EXTRA_LAZY          │
├──────────────────────────────┤
│ count($collection)          │
│          ↓                  │
│ SELECT COUNT(...)           │
├──────────────────────────────┤
│ Не загружать все элементы   │
│ большой collection          │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Финальный экранный текст

| Элемент | Текст |
|---|---|
| Lazy | `Нет обращения → relation не загружен` |
| Риск | `1 query + N queries` |
| Решение | `findRecentWithItems() → fetch join в репозитории` |
| Extra lazy | `count() без полной загрузки collection` |

## Анимация

- В состоянии lazy подсвечивается выполняемая строка PHP, затем появляется
  единственный `SELECT`; счётчик `items` остаётся на нуле.
- На `38:11.07` начинается N+1: SQL-строки появляются вслед за итерациями,
  счётчик растёт `1 → 2 → 3 → … → 1 + N`.
- На `38:20` появляется прикладной вызов `findRecentWithItems()`, затем его
  реализация через QueryBuilder и один SQL с join.
- В EXTRA_LAZY счётчик отделяется от полной коллекции.

## Shorts

- Хук Shorts: `Чем LAZY отличается от EXTRA_LAZY в Doctrine?`
- Повреждение звука не влияет на графику полной или вертикальной версии.

## Материалы и производство

- Для первых трёх учебных состояний нужен общий компонент `PHP/DQL | SQL log`
  с покадровой подсветкой строк и счётчиком запросов.
- Новые изображения, досъёмка и синтетическая озвучка не нужны.

## Ревью

- [ ] EXTRA_LAZY не обещает больше, чем отдельные collection operations.
- [ ] SQL в N+1 читается на ноутбуке.
- [ ] Таймкоды сверены по оригиналу.
- [ ] Оба aspect ratio имеют самостоятельную компоновку.
- [ ] Пользователь принял план.
