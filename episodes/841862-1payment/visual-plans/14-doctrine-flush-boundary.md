# Визуальный план вопроса 14 — «Почему flush обычно вызывают один раз?»

Статус: `APPROVED`  
Сложность: `M`  
Бюджет реализации: `20–30 минут`  
Shorts: `да`

## Источники и границы

- `questions.txt`, пункт 23.
- `transcription.txt`, ориентир `37:41–38:05`; ремонт повреждённого звука
  выполняется отдельно и не влияет на карточку вопроса.
- [Doctrine ORM — Architecture](https://www.doctrine-project.org/projects/doctrine-orm/en/current/reference/architecture.html).
- [Doctrine ORM — Transactions and Concurrency](https://www.doctrine-project.org/projects/doctrine-orm/en/current/reference/transactions-and-concurrency.html).

## Аудит содержания

| Тезис | Оценка | Экранное действие |
|---|---|---|
| Unit of Work накапливает изменения | корректно | показать одну пачку изменений |
| Один `flush()` уменьшает число отдельных синхронизаций | корректное практическое правило; число SQL не обязано уменьшиться | показать один boundary и одну transaction |
| `flush()` должен быть строго один на HTTP-request | слишком жёстко | уточнить: один на логическую единицу работы, если нет причины делить её |

Не углубляемся в порядок SQL, batch processing и ручные savepoints.

### Намеренно не показываем

- Внутренний commit order Doctrine и количество конкретных SQL-запросов.
- Несколько `flush()` как безусловную ошибку: бывают обоснованные границы.

## Задача визуализации

Показать `flush()` как конец логической единицы работы, а не магическое
требование «ровно один раз на HTTP-request».

## Состояния и тайминг

| Состояние | Таймкод | Функция |
|---|---|---|
| 1. Вопрос | `37:41–37:45` | показать постановку вопроса |
| 2. Граница Unit of Work | `37:45–38:05` | показать смысл одного `flush()` и оговорку |

```mermaid
flowchart LR
  Q["1 · Вопрос"] --> U["2 · Одна логическая Unit of Work"]
```

## Состояние 1 — вопрос

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 14/32  Почему flush обычно вызывают один раз за запрос?      │
├──────────────────────────────────────────────────────────────┤
│ Михаил · слушает                      Интервьюер · говорит  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 14/32                       │
│ Почему один flush?          │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Состояние 2 — одна граница записи

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 14/32  Одна логическая операция                              │
├──────────────────────────────────────────────────────────────┤
│ create Order  →  reserve Payment  →  add Event              │
│       все изменения накапливает Unit of Work                 │
│                              ↓ flush()                       │
│                    BEGIN · SQL… · COMMIT                     │
├──────────────────────────────────────────────────────────────┤
│ Меньше границ транзакций · нет промежуточного состояния      │
│ Уточнение ответа: это правило, а не запрет на несколько flush│
├──────────────────────────────────────────────────────────────┤
│ Михаил · говорит                      Интервьюер · слушает  │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 14/32 · Unit of Work        │
├──────────────────────────────┤
│ create → change → event     │
│            ↓                │
│         flush()             │
│            ↓                │
│ BEGIN · SQL · COMMIT        │
├──────────────────────────────┤
│ Одна логическая операция    │
│ ≠ безусловно один flush     │
├──────────────────────────────┤
│ Михаил        Интервьюер     │
└──────────────────────────────┘
```

## Анимация и Shorts

- Изменения по очереди входят в Unit of Work; только на слове `flush` пачка
  уходит в транзакцию.
- Хук Shorts: `Один flush — не закон Doctrine, а хорошая граница операции`.

## Финальный экранный текст

| Элемент | Текст |
|---|---|
| Механизм | `Unit of Work накапливает изменения` |
| Граница | `flush() → одна transaction` |
| Уточнение | `Один раз — правило по умолчанию, не запрет` |

## Материалы и производство

- Использовать существующие pipeline/card-компоненты.
- При реализации сверить переход с оригинальным звуком.
- Новые изображения не нужны; досъёмка и синтетический звук не планируются.

## Ревью

- [x] Повреждение звука не влияет на экранную графику.
- [ ] Уточнение не звучит как запрет нескольких `flush()`.
- [ ] 16:9 и 9:16 читаются без мелкого текста.
- [ ] Таймкоды сверены с оригинальным звуком.
- [ ] Пользователь принял план до верстки.
