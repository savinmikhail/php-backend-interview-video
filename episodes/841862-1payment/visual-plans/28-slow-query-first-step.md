# Визуальный план вопроса 28 — «Что делать с медленным запросом?»

Статус: `APPROVED`  
Сложность: `S`  
Бюджет реализации: `15 минут`  
Shorts: `да, короткий`

## Источники и границы

- `questions.txt`, пункт 40.
- `transcription.txt`, `57:39–58:03`.
- [PostgreSQL — Using EXPLAIN](https://www.postgresql.org/docs/current/using-explain.html).
- [PostgreSQL — Examining Index Usage](https://www.postgresql.org/docs/current/indexes-examine.html).

## Аудит содержания

Ответ `EXPLAIN ANALYZE` правильный и достаточный для quick-fire. Экран добавляет
`BUFFERS` и два предохранителя: анализировать реальный запрос/параметры и
помнить, что `ANALYZE` действительно выполняет запрос.

### Намеренно не показываем

- «Добавить индекс» как автоматический первый ответ.
- Полный PostgreSQL plan и настройку сервера без наблюдений.
- Оптимизацию ORM до измерения реального SQL.

## Задача визуализации

Закрепить диагностический порядок: получить фактический plan, увидеть узкое
место, только потом менять запрос, индекс или данные.

## Состояние 0 — вопрос

Принято пользователем: стандартная карточка `28/30` на review
`41:55–42:14`, source `57:38–57:57` по конвертеру текущего монтажа.
Текст: `Таблица 10 млн строк стала медленной после фильтра. Что делать?`
В 16:9 крупный вопрос по центру, счётчик сверху; в 9:16 стандартная
вертикальная компоновка вопроса с переносами. Ответ не показывается заранее.

## Состояние 1 — ответ

Существующий экран с EXPLAIN сохранён на source `57:57–58:03`,
review `42:14–42:20`. Содержание ответа и конец сегмента не меняются.

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 28/30  10 млн строк · фильтр стал медленным                  │
├─────────────────────────────┬────────────────────────────────┤
│ ПЕРВЫЙ ШАГ                  │ СМОТРИМ В ПЛАНЕ                │
│ EXPLAIN (ANALYZE, BUFFERS)  │ actual time · rows · loops    │
│ SELECT ... WHERE status = ? │ Seq/Index Scan · buffers      │
├─────────────────────────────┴────────────────────────────────┤
│ ANALYZE выполняет запрос · сначала безопасная среда          │
└──────────────────────────────────────────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 28/30 · МЕДЛЕННЫЙ QUERY     │
├──────────────────────────────┤
│ EXPLAIN                    │
│ (ANALYZE, BUFFERS)         │
├──────────────────────────────┤
│ actual rows / time / loops │
│ scan type / buffers        │
├──────────────────────────────┤
│ ANALYZE выполняет запрос    │
└──────────────────────────────┘
```

## Состояние 2 — реальный кейс и QR

Принято пользователем: отдельная карточка на свободном разговорном промежутке
source `58:03–58:09`, review `42:20–42:26`. Она не перекрывает ответ и исчезает
до начала вопроса про SOLID.

Польза: показать конкретное продолжение темы и дать доступ к реальному plan без
перегрузки шестисекундного учебного экрана.

QR ведёт прямо на пост `https://t.me/msavin_php/52`. Инвайт-ссылка канала в
кадре не используется: зритель должен сразу получить обещанный разбор.

### 16:9

```text
┌──────────────────────────────────────────────────────────────┐
│ 28/30  Реальный кейс · EXPLAIN ANALYZE                      │
├──────────────────────────────────┬───────────────────────────┤
│                                  │                           │
│  16,2 с  →  250 мс               │        [ QR-КОД ]         │
│                                  │                           │
│  JIT занимал 16 из 16,2 секунды  │  План запроса и разбор   │
│  В этом запросе помог            │  @msavin_php · пост №52  │
│  SET jit = off                   │                           │
└──────────────────────────────────┴───────────────────────────┘
```

### 9:16

```text
┌──────────────────────────────┐
│ 28/30 · РЕАЛЬНЫЙ КЕЙС       │
│ EXPLAIN ANALYZE              │
├──────────────────────────────┤
│ 16,2 с                       │
│      ↓                       │
│ 250 мс                       │
│                              │
│ JIT: 16 из 16,2 секунды     │
│ В этом запросе помог         │
│ SET jit = off               │
│                              │
│         [ QR-КОД ]           │
│ План запроса и разбор        │
│ @msavin_php · пост №52       │
└──────────────────────────────┘
```

## Финальный экранный текст

| Элемент | Текст |
|---|---|
| Первый шаг | `EXPLAIN (ANALYZE, BUFFERS)` |
| Метрики | `actual time · rows · loops · buffers` |
| Осторожно | `ANALYZE выполняет запрос` |
| Кейс | `16,2 с → 250 мс` |
| Наблюдение | `JIT занимал 16 из 16,2 секунды` |
| Ограничение | `В этом запросе помог SET jit = off` |
| CTA | `План запроса и разбор · @msavin_php · пост №52` |

## Анимация

- Из SQL выделяется plan node с самым большим actual time.
- QR и CTA появляются после метрики и остаются неподвижными для сканирования.

## Shorts

- Хук: `Не добавляй индекс вслепую — сначала посмотри фактический план`.

## Материалы и производство

- Использовать SQL + plan-summary component.
- QR генерируется локально из `https://t.me/msavin_php/52` и хранится как
  статический SVG в Remotion `public/`.
- Другие изображения, исправление ответа и досъёмка не нужны.

## Ревью

- [ ] Команда и предупреждение читаемы целиком.
- [ ] Не обещается, что индекс обязательно решит проблему.
- [ ] 9:16 сохраняет actual metrics.
- [ ] Таймкод проверен.
- [ ] QR открывает Telegram-пост №52.
- [ ] Пользователь принял план.
